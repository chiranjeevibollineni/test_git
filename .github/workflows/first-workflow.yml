name: my-first-workflow

on:
  push:

jobs:

  runs-on: ubuntu-latest

  steps:
    name: checkout-code
    uses: actions/checkout@v4

    name: print hello
    run: echo "hello git hub actions"